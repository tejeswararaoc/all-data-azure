1 First we need to install nginx in ubuntu refer below url:-
sudo apt update
sudo apt install nginx 

2.sudo ufw app list 
3.sudo ufw allow 'Nginx HTTP' 
4.sudo ufw status 
5.systemctl status nginx 
6.http://your_server_ip 
7.sudo systemctl stop nginx 
8.sudo systemctl start nginx 
9.sudo systemctl restart nginx 
10.sudo systemctl reload nginx 
11.sudo systemctl enable nginx 
12.REfer this url:_ https://www.digitalocean.com/community/tutorials/how-to-install-nginx-on-ubuntu-20-04  

SSL CErtification configuration follow the below url:-
###NGINX SSL Configuration Step by Step Details - Ubiq BI  

cd /etc/nginx/
here mkdir ssl
here copy the ssl certificate with path.

now configure the proxy in below path.

cd /etc/nginx/sites-available/
nano default

here copy the below data completely and change the doamin based on our requirement.


# This is the server block for your Nginx reverse proxy
server {
    # The server listens on port 443 for SSL encrypted traffic
    listen 443 ssl;

    # Replace 'your_domain.com' with your actual domain name
    server_name qc.kshema.co;
 
    # Paths to the SSL certificate and key files
    # Replace with the actual paths if different
    ssl_certificate /etc/nginx/ssl/kshema.crt;
    ssl_certificate_key /etc/nginx/ssl/kshema.rsa;
 
    # Location block to handle requests
    location / {
        # The proxy_pass directive is set to the Sonar application running on localhost at port 9000
        proxy_pass http://localhost:9000;

        # The following directives set various headers to be passed to the proxied server
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen  80 default_server;
    listen  [::]:80 default_server;
    server_name localhost;

    location / {
        return 301 https://$host$request_uri;
    }
} 








    

    
---------------------

nano /etc/nginx/sites-available/ <--
vi default <-- 

server {
    listen 443 ssl;
    server_name 4.188.184.42;

    ssl_certificate /path/to/your/certificate.pem;
    ssl_certificate_key /path/to/your/private-key.pem;

    location / {
        proxy_pass http://localhost:8080;  # Assuming Tomcat is on the default port
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}



we have copy the above code and modify the port details also



